/**
 * Running a session. Pure, like `src/core` — the screens above supply the DOM
 * and the store supplies persistence; nothing here touches either.
 */

export * from "./session.js";
export * from "./multiplication.js";
