# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Audio fetch cache: what Commons said about each word, so a re-run does not
# repeat thousands of API calls. Derived data — the recordings themselves and
# their credits are committed, this is not.
.audio-cache.json

# Private operating notes for Claude — not project documentation.
# Project docs live in README.md and DESIGN.md.
CLAUDE.md

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Claude Code local settings and tooling
.claude/settings.local.json
.claude/launch.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
